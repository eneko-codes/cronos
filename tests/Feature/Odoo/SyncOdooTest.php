<?php

describe('Odoo sync', function (): void {
    it('runs the Odoo sync flow end-to-end (placeholder)', function (): void {
        // This would require faking Odoo API responses and asserting DB state
        expect(true)->toBeTrue();
    });
});
